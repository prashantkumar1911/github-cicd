name: Experience CI/CD
on:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Debug via SSH
        uses: mxschmitt/action-tmate@v3
        with:
          limit-access-to-actor: true